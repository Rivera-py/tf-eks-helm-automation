name: 'AWS - Dev - Terraform Test'

on:      
  push:
    branches: [ "feature/*" ]
  pull_request:
    branches: [ "feature/*" ]

permissions:
  contents: read
  id-token: write
  checks: write
  pull-requests: write
  issues: write

defaults:
  run:
    shell: bash

jobs:
  aws-terraform-test:
    name: 'Plan and Test Terraform - aws - dev'
    uses: ./.github/workflows/terraform.yaml
    with:
      environment: dev
      aws_region: eu-west-2